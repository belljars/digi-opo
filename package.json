{
  "name": "digi-opo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "build:exe:win": "scripts\\win\\build_exe.bat",
    "check": "tsc --noEmit --project tsconfig.json",
    "run": "node scripts\\win\\run-app.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
